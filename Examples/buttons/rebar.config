{erl_opts, [debug_info]}.
{deps, []}.

{escript_incl_apps,
 [buttons]}.
{escript_main_app, buttons}.
{escript_name, buttons}.
{escript_emu_args, "%%! +sbtu +A1 -env TCLLIBPATH priv\n"}.

%% Profiles
{profiles, [{test,
             [{erl_opts, [debug_info]}
            ]}]}.
